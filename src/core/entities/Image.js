class Image {
    constructor (id, imageData, width, height) {
        this.id = id;
        this.imageData = imageData;
        this.width = width;
        this.height = height;
    }
}

module.exports = Image;